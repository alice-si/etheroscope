apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: etheroscope-serwer
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: etheroscope-serwer
  template:
    metadata:
      labels:
        app: etheroscope-serwer
    spec:
      terminationGracePeriodSeconds: 30
      volumes:
      - name: rinkeby-geth-volume
        gcePersistentDisk:
          pdName: rinkeby-geth-disk
          fsType: ext4
      containers:
      - name: etheroscope-serwer-container
        image: gcr.io/etheroscope/etheroscope-serwer:latest
        imagePullPolicy: "Always"
        volumeMounts:
        - name: rinkeby-geth-volume
          mountPath: /usr/src/app/etheroscope/geth-blockchains/fullRinkebyBlockchain/
